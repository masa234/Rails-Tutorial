= form_for(micropost.likes.build, remote: true)  do |f|
  = hidden_field_tag :micropost_id, micropost.id
  = button_tag(class: "btn btn-default btn-sm") do
    = content_tag :span, "いいねする", class: "glyphicon glyphicon-heart-empty"
  